# --- Obsidian: version settings + plugins (so other devices inherit the
# --- vault's config: properties-hidden, Dataview, Breadcrumbs, graph
# --- groups), but never per-device workspace caches.
Jidousha/.obsidian/workspace.json
Jidousha/.obsidian/workspace-mobile.json
Jidousha/.obsidian/cache/

# Obsidian's trash folder
Jidousha/.trash/

# fallback folder for files awaiting manual deletion
_to_delete/

# OS / editor junk
.DS_Store
.directory
*~

# tooling caches
__pycache__/
*.pyc
